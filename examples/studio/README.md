# Studio Docker Compose example

See [docker-compose.yml](./docker-compose.yml). Mounts a volume at `/data` and serves Studio on port 4310.

Requires building/publishing `agent-inspect` or adjusting the image command for local monorepo dev.
