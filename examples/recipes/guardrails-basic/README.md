# Guardrails basic

Deterministic local guardrail checks on sample text and JSON using `@agent-inspect/guardrails`.

```bash
pnpm install
pnpm start
```

No network calls. Results are advisory, not compliance certification.
