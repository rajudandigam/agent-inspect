# Phoenix / OpenInference import (local)

Experimental OpenInference export for **local review**. No Phoenix dependency. No upload.

```bash
pnpm install
pnpm start
```

Then run the printed `agent-inspect export` command. Import into Phoenix only after redaction review.

See [docs/STANDARDS.md](../../../docs/STANDARDS.md).
