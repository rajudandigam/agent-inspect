# Langfuse local import (self-hosted)

Export OTLP JSON locally for optional import into **your** Langfuse instance. No Langfuse SDK. No upload by default.

```bash
pnpm install
pnpm start
```

See [docs/STANDARDS.md](../../../docs/STANDARDS.md).
