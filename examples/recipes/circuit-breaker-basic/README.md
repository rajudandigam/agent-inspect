# Circuit breaker basic

Analyze a synthetic tool-call sequence with `@agent-inspect/circuit` repetition rules.

```bash
pnpm install
pnpm start
```

Circuits return evidence only; they do not stop agents unless your code enforces results.
