After v0.3 spike:
GO if the tree helps debug faster than raw logs.
NO-GO if output is confusing or no better than logs.

After full v0.3:
GO if 2+ developers find it useful on real logs.
NO-GO if no adoption after 2 weeks.

After v0.4:
GO if live tail is preferred over raw logs in at least one workflow.

After v0.5:
GO if LangChain adapter produces better traces than logs alone.

Before v1.0:
GO only if 5+ teams use it in real workflows, schema is stable, docs are strong, and no P0 bugs remain.