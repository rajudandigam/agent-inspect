# Suite viewer fixtures

Use `fixtures/configs/outcome-suite.suite.json` with the suite viewer:

```bash
agent-inspect viewer --suite --config fixtures/configs/outcome-suite.suite.json
```

Open `http://127.0.0.1:7337/?mode=suite` for the HTML sections (case status, tool path, observations, CI artifacts, bundle export hint).
