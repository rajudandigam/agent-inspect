# Cursor prompt — v1.4.0 Chunk 1: CI artifact recipe and trace export workflow polish

Copy everything below the line into a new Cursor agent session on the `agent-inspect` repository.

---

You are working on the existing AgentInspect repository.

**Phase:** v1.4.0 Chunk 1 — CI artifact recipe and trace export workflow polish.

**Goal:** Document and exemplify local-first CI trace artifact workflows. No new reporter package. No runtime behavior changes unless a tiny export/docs bug is proven by audit.

## Read first

- `README.md`, `ROADMAP.md`, `CHANGELOG.md`
- `docs/CLI.md`, `docs/EXPORTS.md`, `docs/SAFE-TRACE-SHARING.md`
- `docs/implementation/V1.4.0-IMPLEMENTATION-PLAN.md`
- `packages/cli/src/export.ts`
- `examples/recipes/` (existing patterns)
- `scripts/recipes-check.mjs` (if present)

## Product boundary (do not violate)

AgentInspect is the **local-first trace workbench** for TypeScript AI agents.

- Local-first, CLI-first, TypeScript-first
- Safe by default, dependency-light
- No vendor upload by default
- No SaaS/dashboard scope
- No universal monkey-patching
- No default replay engine
- No cost engine in core
- No raw chain-of-thought capture
- Do not add dependencies
- Do not implement v1.4.0 Chunk 2 (`timeline`) or Chunk 3 (`stats`) in this step
- Do not implement CI reporter npm package
- Do not add OTLP HTTP sink or network upload behavior to core

## Scope — IN

1. **`docs/CI-ARTIFACTS.md`**
   - When to enable tracing in CI (`AGENT_INSPECT=1`, `inspectRun` / `maybeInspectRun`)
   - Where traces land (`.agent-inspect/`, `AGENT_INSPECT_DIR`)
   - Exporting share-safe artifacts: `agent-inspect export --redaction-profile share` (and `strict` when appropriate)
   - Formats: json, markdown, html (use existing CLI flags)
   - GitHub Actions artifact upload as an **example** (user-controlled upload of local files — not a product feature)
   - Link to `docs/SAFE-TRACE-SHARING.md` — no compliance overclaims

2. **`examples/recipes/github-actions-artifact/`**
   - Minimal deterministic example (no external LLM calls unless mocked/fixture-based)
   - Sample `.github/workflows/agent-inspect-artifact.yml` (or workflow snippet in README)
   - `AGENT_INSPECT=1` usage
   - Steps: run traced test → export → upload artifact
   - Register in `examples/recipes/README.md` if other recipes are indexed there

3. **Export artifact examples**
   - Small sample outputs or scripts showing markdown/html/json export for CI logs
   - Prefer documenting CLI commands over new code

4. **Tiny fixes only**
   - If audit finds export flag docs mismatch or `recipes:check` gap, fix minimally

## Scope — OUT

- `@agent-inspect/vitest-reporter` or similar package
- `timeline` / `stats` CLI commands
- NestJS/logging packages
- Vercel AI SDK adapter
- Default `schemaVersion: "0.2"` writing
- New runtime tracing features
- Network calls from core/library code (GitHub upload in YAML example only)

## Phase 0 — Audit before editing

Report:

1. Whether `docs/CI-ARTIFACTS.md` already exists
2. Whether a github-actions recipe already exists
3. Whether `export --redaction-profile` is documented in CLI.md / EXPORTS.md
4. Whether `recipes:check` covers new recipe paths
5. Files you plan to create or modify

Do not skip the audit.

## Implementation

After audit, implement only what is missing. Match existing recipe style (deterministic, no secrets, no external services by default).

## Tests / validation

```bash
pnpm build
pnpm typecheck
pnpm test
pnpm recipes:check
pnpm fixtures:check
```

Add or update recipe check entries if the repo has a recipes manifest.

## Docs updates

- Link `docs/CI-ARTIFACTS.md` from README Documentation section and/or ROADMAP v1.4.0 row
- Do not bump package version
- Do not publish
- Do not add CHANGELOG release date

## Final report

- Audit summary
- Files created / modified
- Validation commands run and results
- Confirmation: no reporter package, no timeline/stats, no vendor upload in core, no version bump, no publish
