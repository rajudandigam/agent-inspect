# v2.6.0 Execution Plan

## Train

```yaml
train: "v2.6.0"
theme: "Optional viewer and IDE/MCP surfaces"
baselineVersion: "previous published train"
executionMode: "codex-autonomous-release-train"
```

## Goal

Add optional read-only surfaces only after core utility adoption exists.

## Product boundaries

This train must preserve:

- local-first behavior;
- no default network calls;
- no root/core framework or provider dependencies;
- no raw chain-of-thought capture;
- no provider pricing engine;
- no hosted service or dashboard;
- deterministic behavior by default;
- old trace readability;
- package ESM/CJS/declaration compatibility.

## Ordered chunks

### Chunk 0 — surface demand review and viewer RFC

**Goal:** Verify demand for viewer/MCP/IDE surfaces and freeze read-only scope.

**In scope**
- adoption metric review
- viewer read-only RFC
- MCP server scope
- IDE decision gate

**Out of scope**
- viewer implementation
- mutating MCP tools
- IDE extension without demand

**Likely files**
- `docs/product/ADOPTION-METRICS.md`
- `docs/proposals/LOCAL-VIEWER.md`
- `docs/proposals/READ-ONLY-MCP-SERVER.md`

**Tests / fixtures**
- docs gate

**Success criteria**
- demand documented
- read-only/non-mutating scope fixed

**Suggested commit**

```text
docs: define optional viewer and MCP surface scope
```

### Chunk 1 — @agent-inspect/viewer package

**Goal:** Add optional localhost-only read-only viewer package.

**In scope**
- agent-inspect serve
- localhost bind
- tree
- timeline
- reports
- diff
- check results
- session browsing
- safe-sharing status

**Out of scope**
- cloud hosting
- account
- database in root
- trace mutation
- write APIs

**Likely files**
- `packages/viewer/package.json`
- `packages/viewer/src/`
- `packages/viewer/test/`
- `packages/cli/src/serve.ts`
- `tsup.viewer.config.ts`

**Tests / fixtures**
- read-only route tests
- localhost bind tests
- package smoke

**Success criteria**
- viewer is optional
- read-only
- no upload
- source JSONL remains canonical

**Suggested commit**

```text
feat(viewer): add optional local trace viewer
```

### Chunk 2 — read-only MCP server

**Goal:** Expose AgentInspect traces as safe read-only MCP tools.

**In scope**
- list traces
- read trace
- search traces
- find first error
- find slowest path
- compare runs
- run checks
- create share-safe report

**Out of scope**
- trace mutation
- tool invocation
- unredacted data by default
- replay
- auto-fix
- automatic code edits

**Likely files**
- `packages/mcp-server/package.json`
- `packages/mcp-server/src/`
- `packages/mcp-server/test/`
- `examples/recipes/read-only-mcp-server/`

**Tests / fixtures**
- MCP server mock tests
- safe redaction tests
- package smoke

**Success criteria**
- all tools are read-only
- unredacted data protected by default

**Suggested commit**

```text
feat(mcp-server): add read-only trace tools
```

### Chunk 3 — IDE integration decision and docs

**Goal:** Document whether Cursor/VS Code surface should be deferred or scaffolded.

**In scope**
- demand review
- extension user stories
- defer/scaffold decision
- no implementation unless demand gate passed

**Out of scope**
- IDE extension without demand
- automatic code edits

**Likely files**
- `docs/product/ADOPTION-METRICS.md`
- `docs/IDE-SURFACES.md`
- `docs/implementation/release-trains/V2.6.0-EXECUTION-PLAN.md`

**Tests / fixtures**
- docs gate

**Success criteria**
- decision is explicit
- no visibility-only extension

**Suggested commit**

```text
docs: record IDE surface decision
```

### Chunk 4 — viewer/MCP docs and release readiness

**Goal:** Finalize optional surface docs and release gate.

**In scope**
- README
- CLI
- ADAPTERS
- SECURITY
- readiness

**Out of scope**
- publish before authorization

**Likely files**
- `README.md`
- `docs/CLI.md`
- `docs/SECURITY.md`
- `docs/implementation/release-trains/V2.6.0-RELEASE-READINESS.md`

**Tests / fixtures**
- full release gate
- package smoke for viewer/MCP server

**Success criteria**
- optional surfaces are clearly optional/read-only
- all packages smoke pass

**Suggested commit**

```text
docs: prepare v2.6 release readiness
```


## Train validation

Run the appropriate focused gates per chunk. Before release readiness, run:

```bash
pnpm install --frozen-lockfile
pnpm build
pnpm typecheck
pnpm test
pnpm test:coverage
pnpm fixtures:check
pnpm recipes:check
pnpm size
pnpm test:all
pnpm pack:smoke
pnpm compat:smoke
npm pack --dry-run
git diff --check
```

Additional train-specific validation:

- viewer localhost/read-only tests;
- MCP server read-only tests;
- no unredacted data default tests;
- no root dependency bloat.

## Release readiness

Create/update:

```text
docs/implementation/release-trains/v2.6.0-RELEASE-READINESS.md
```

Release notes should cover:

- viewer and read-only MCP package status;
- IDE decision;
- security posture;
- optional package boundaries.

Do not publish until maintainer explicitly authorizes release prep.
