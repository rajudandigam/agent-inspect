# V1.8.1 Execution Plan

## Train

```yaml
train: "v1.8.1"
theme: "Documentation truth and adoption polish"
executionMode: "autonomous-release-train"
branch: "main"
```

## Ordered chunks

### Chunk 1 — docs-roadmap-maintainer-cleanup

Scope:

- make `docs/implementation/ROADMAP-V1.8.1-TO-V3.md` the active maintainer roadmap;
- update AGENTS, Codex maintainer guidance, release-train state, and current task files;
- add public and implementation docs indexes;
- mark old Cursor/Codex operational files as historical without deleting release evidence;
- align public docs around `observe()`, framework adapters, advanced structured-log ingestion, root imports, subpaths, and safe sharing;
- remove repository noise such as `docs/.DS_Store`.

Out of scope:

- runtime source changes;
- package version changes;
- changesets;
- dependency changes;
- npm publishing, tags, or GitHub releases;
- hosted upload, dashboard, replay, cost-engine, or default network behavior.

Focused tests:

```bash
pnpm typecheck
pnpm test
git diff --check
```

Acceptance criteria:

- public README and getting-started docs show `observe()` before manual `inspectRun` / `step`;
- public docs agree that the current npm release is 1.8.0;
- root import guidance uses stable beginner APIs;
- advanced examples use subpaths;
- safe sharing states no upload by default, redaction before disk, and redaction before export;
- state/task files identify this v1.8.1 docs cleanup as complete after validation.

Stop conditions:

- validation failure that cannot be repaired in this docs-only scope;
- unrelated worktree changes;
- any need for runtime, schema, package-version, dependency, release, tag, publish, or network-behavior changes.
