# Test matchers

Experimental Vitest and Jest TraceContract matchers (`toPassTraceContract`, `toHaveRequiredTool`) ship in `@agent-inspect/vitest` and `@agent-inspect/jest`.

See the matcher section on the canonical [Trace contracts](/docs/trace-contracts/) page ([TRACE-CONTRACTS.md](./TRACE-CONTRACTS.md)). This route no longer duplicates that body.
