# Cursor prompt: v1.2.0 PR 2 — Core PersistedInspectEvent types and validators

Copy everything below the line into a new Cursor session.

---

You are working on the existing AgentInspect repository.

**Current maintainer phase:** v1.2.0 release train — **PR 2** (types and validator only)

**Goal:** Add core `PersistedInspectEvent` types and runtime validation. No converters, storage, or CLI changes.

**Do not** implement converters, storage read/write, CLI integration, LangChain changes, or publish.

---

## Product boundary

AgentInspect is the **local-first trace workbench** for TypeScript AI agents. It is not a SaaS, dashboard, production APM replacement, replay engine, cost engine, or universal monkey-patching framework.

---

## Read first

- `docs/proposals/UNIFIED-PERSISTED-INSPECT-EVENT.md` (§4 Proposed model)
- `docs/proposals/UNIFIED-PERSISTED-INSPECT-EVENT-CHECKLIST.md` (PR 2 section)
- `docs/implementation/CURSOR-MAINTAINER-ROADMAP.md`
- `packages/core/src/types/inspect-event.ts` (existing `InspectKind`, `AttributionConfidence`)
- `packages/core/src/types.ts` (`TraceEvent`, `TraceSchemaVersion`)
- `packages/core/src/index.ts` (export patterns)
- `packages/core/test/types/inspect-event.test.ts` (test style)
- `packages/core/test/api-stability.test.ts`

---

## Phase 0 — Audit before editing

Report:

1. Root `package.json` version.
2. Whether `packages/core/src/types/persisted-inspect-event.ts` already exists.
3. Whether `PersistedInspectEvent` is already exported from `index.ts`.
4. Whether `isPersistedInspectEvent` or converters already exist.
5. Exact files you plan to create or modify.

Do not skip the audit.

---

## In scope

- Create `packages/core/src/types/persisted-inspect-event.ts` with:
  - `PersistedSchemaVersion` literal `"0.2"`
  - `PersistedEventSource` with `type: "manual" | "json-log" | "log4js" | "adapter" | "ai-sdk" | "otel"` plus optional `name` / `version`
  - `PersistedInspectEvent` matching the proposal doc (ISO `timestamp` string; optional fields per proposal)
  - Reuse existing `InspectKind` and `AttributionConfidence` from `inspect-event.ts` (import, do not duplicate enums)
- Implement `isPersistedInspectEvent(value: unknown): value is PersistedInspectEvent` — strict runtime validator
- Export types and `isPersistedInspectEvent` from `packages/core/src/index.ts`
- Add `packages/core/test/types/persisted-inspect-event.test.ts`
- Update `api-stability.test.ts` if the public export list changes

---

## Out of scope

- `traceEventToPersisted` / `inspectEventToPersisted` converters (PR 3–5)
- `writeTraceEvent`, `readTraceEvents`, dual-read, or `0.2` disk writes (PR 6–7)
- CLI `view` / `export` / `diff` changes
- LangChain adapter changes
- `schemaVersion: "0.1"` write behavior changes
- New dependencies
- Version bump, changeset, publish, tag, release
- Modifying `fixtures/traces/*.jsonl`

---

## Tests to add

| Test | Expectation |
| ---- | ----------- |
| Valid minimal event | Passes `isPersistedInspectEvent` |
| Wrong/missing `schemaVersion` | Fails |
| Missing required fields | Fails (`eventId`, `runId`, `kind`, `name`, `timestamp`, `confidence`, `source`) |
| Optional fields | `parentId`, `attributes`, `error`, `tokenUsage`, `trace` accepted when valid |
| `source.type` union | Each allowed type accepted; invalid type rejected |
| `status` union | `running` / `ok` / `error` / `unknown` when present |

---

## Validation commands

```bash
pnpm build
pnpm typecheck
pnpm test
```

---

## Final report required

1. Audit summary
2. Files created / modified
3. Export surface added to `index.ts`
4. Test count added
5. Validation commands run and results
6. Confirmation: no converters, no storage/CLI changes
7. Confirmation: `0.1` write path unchanged
8. Confirmation: no version bump / publish
9. Recommendation: next chunk is PR 3 (`0.1` TraceEvent converters)

---

## Hard constraints

- Types + validator only
- No `schemaVersion: "0.1"` behavior changes
- No new dependencies
- No publish / no version bump unless explicitly instructed later
