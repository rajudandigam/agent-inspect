# 6.17.8+ queue (read-only) — prepared after 6.17.7 publish

**Mode:** plan-only / comment drafts. Do **not** close or merge from this file without an explicit maintainer post.

Generated: 2026-09-04 after Trusted Publish of `6.17.7`.

## PR queue table

| PR | Author | Train | Disposition | Draft comment |
| ---: | --- | --- | --- | --- |
| #315 | HsienW | 6.20 | Keep open; REQUEST CHANGES naming remains | See draft A |
| #314 | HsienW | 6.17.8 reassess | Rebase onto `main` after 6.17.7; reassess multi-manifest + fail propagation | See draft B |
| #307 | HsienW | 6.18 | Rebase; no-key AI SDK packed golden path | See draft C |
| #306 | HsienW | 6.17.8 | Consolidate with #297 into one PR | See draft D |
| #305 | abhay-codes07 | triage | Compare to current `main`; assign train or request rebase | See draft E |
| #303 | abhay-codes07 | triage | Docs recipe; Vercel auth ≠ code signal | See draft E |
| #302 | abhay-codes07 | triage | Compare to current MCP fixtures on `main` | See draft E |
| #297 | HsienW | 6.17.8 | Consolidate with #306 | See draft D |
| #296 | abhay-codes07 | triage | Doctor messaging; rebase request if stale | See draft E |
| #295 | abhay-codes07 | triage | VS Code sample command — product scope check | See draft E |
| #294 | abhay-codes07 | triage | TraceFacts parity — may already be covered | See draft E |
| #142 | abhay-codes07 | triage | Older recipe PR — rebase or close as superseded after compare | See draft E |
| #311 | (issue) | 6.18 | Stay open — preview parity prerequisites | n/a |
| #308 / #309 | (issues) | 6.20 | Stay open; label `roadmap-future` | n/a |
| #320 / #321 | (issues) | 6.21 | Stay open; label `roadmap-future` | n/a |

## Exact comment drafts (maintainer posts)

### Draft A — #315 (6.20)

```text
Thanks for the requiredOrderMode work. This stays scheduled for the 6.20 contract train (not 6.17.x). Please keep the PR open; naming/API surface still needs the REQUEST CHANGES items resolved when that train starts. No need to rebase until 6.20 is active unless you prefer staying current with main.
```

### Draft B — #314 (reassess after rebase)

```text
Thanks — marking this for 6.17.8 queue health. Please rebase onto current `main` (now includes 6.17.7). After rebase we will reassess the multi-manifest + fail-propagation coverage. Vercel auth failures are not treated as code signals.
```

### Draft C — #307 (6.18)

```text
Thanks for the no-key AI SDK packed golden path. This is queued for 6.18 adapter capture parity. Please rebase onto current `main` when convenient; we will not merge ahead of the 6.18 train gate.
```

### Draft D — #297 + #306 (consolidate)

```text
For 6.17.8 contributor-queue health, please consolidate #297 and #306 into a single PR (README completeness + prose support bindings) so review stays one concern. Happy to leave both open until the consolidated branch is ready, then close the duplicate.
```

### Draft E — older open PRs (triage, don’t auto-close)

```text
Thanks for the contribution. We are triaging older open PRs against current `main` for the 6.17.8 queue. Please rebase when you can; if the work is already on main or superseded, reply and we will close with a pointer. We will not auto-close without that compare.
```

## Verbatim 6 notes (6.18 — plan only)

- Adapter matrix + Choose-your-capture-path docs
- Rebase #307; hold #311 preview until prerequisites
- Mastra go/no-go gates — no immediate Mastra package

## Verbatim 7 notes (refusal evidence)

- Issue #331 already open (recipe after Patrick confirms)
- Draft Patrick message: confirm model for deterministic guardrail refusal + expected non-action before any recipe implementation; no new event/preset/#321 fields yet

## Label hygiene targets

Apply when posting (do not silently invent labels if missing in repo):

- `#308`, `#309`, `#320`, `#321` → `roadmap-future` (+ maintainer/scheduled if present)
- Keep `#311` on 6.18 track
- Keep `#315` on 6.20 track
