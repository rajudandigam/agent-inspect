# v1.4.0 release readiness

**Status:** Release-readiness checklist — train features implemented locally; **not published**.

**Theme:** CI artifacts, timeline, stats, deterministic search.

---

## Scope included

| Chunk | Delivered |
| ----- | --------- |
| CI artifacts | [CI-ARTIFACTS.md](../CI-ARTIFACTS.md), [github-actions-artifact recipe](../../examples/recipes/github-actions-artifact/) |
| Timeline | `agent-inspect timeline <runId>` |
| Stats | `agent-inspect stats` |
| Search | `agent-inspect search` |

## Scope excluded

- Vitest/Jest reporter packages
- SaaS / dashboard / vendor upload
- OTLP HTTP sink
- Default `schemaVersion: "0.2"` writing
- Semantic/fuzzy search
- Cost engine / replay

---

## Validation commands

```bash
pnpm install --frozen-lockfile
pnpm build
pnpm typecheck
pnpm test
pnpm test:coverage
pnpm size
pnpm test:all
pnpm fixtures:check
pnpm recipes:check
pnpm pack:smoke
pnpm compat:smoke
npm pack --dry-run
node packages/cli/dist/index.cjs timeline --help
node packages/cli/dist/index.cjs stats --help
node packages/cli/dist/index.cjs search --help
```

---

## Manual release checklist (maintainer only)

1. Changeset + version bump when instructed.
2. README / ROADMAP / CHANGELOG alignment.
3. Confirm no network upload in core.
4. Confirm `schemaVersion: "0.1"` write path unchanged.
5. Publish via established process only when explicitly instructed.
