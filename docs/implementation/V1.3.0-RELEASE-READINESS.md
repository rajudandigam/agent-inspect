# v1.3.0 release readiness

**Status:** Release-readiness checklist — train features implemented on `main`; **not published** until maintainer explicit publish instruction.

**Theme:** Correlation metadata, share-safe exports, LangChain streaming metadata.

**Companion:** [V1.3.0-IMPLEMENTATION-PLAN.md](./V1.3.0-IMPLEMENTATION-PLAN.md) · [V1.3.0-RELEASE-TRAIN.md](./V1.3.0-RELEASE-TRAIN.md)

---

## Scope included (v1.3.0)

| Chunk | Delivered |
| ----- | --------- |
| **Correlation metadata** | `correlationId`, `requestId`, `decisionId`, `groupId` on `inspectRun` / `maybeInspectRun`; `getCurrentCorrelationMetadata()`; persisted converter mapping |
| **Redaction profiles** | `local` / `share` / `strict`; trace writing + `export --redaction-profile`; [SAFE-TRACE-SHARING.md](../SAFE-TRACE-SHARING.md) |
| **LangChain streaming** | `stream: true` metadata-only chunk counts/duration; bounded preview; deferred persist for streaming LLM steps |

---

## Scope excluded (deferred)

- `timeline` / `stats` / cohort CLI
- CI reporters / GitHub Actions artifact reporters
- NestJS / logging bridge packages
- Vercel AI SDK adapter
- OTLP HTTP sink / vendor upload
- Default `schemaVersion: "0.2"` writing
- SaaS / dashboard
- Replay / cassette engine
- Cost engine in core

---

## Product boundary confirmations

- [x] Local-first, CLI-first, TypeScript-first
- [x] No vendor upload or network sink added
- [x] Manual traces remain `schemaVersion: "0.1"` event names unchanged
- [x] v0.2 not written by default
- [x] LangChain streaming does not capture full token text by default (`capture: "metadata-only"`)
- [x] Redaction profiles are key-based — not compliance-grade DLP
- [x] Export redaction does not mutate original JSONL traces

---

## Validation commands

Run before publish instruction:

```bash
pnpm install --frozen-lockfile
pnpm build
pnpm typecheck
pnpm test
pnpm test:coverage
pnpm size
pnpm test:all
pnpm fixtures:check
pnpm recipes:check
pnpm pack:smoke
pnpm compat:smoke
pnpm --filter @agent-inspect/langchain test
npm pack --dry-run
```

---

## Manual release checklist (do not run publish until approved)

1. Confirm package versions (bump via Changesets / Version Packages when instructed).
2. Confirm README / ROADMAP / CHANGELOG / ADAPTERS alignment.
3. Confirm no network/vendor upload behavior introduced.
4. Confirm LangChain `stream: true` does not store full token streams by default.
5. Confirm redaction profile docs make no compliance overclaims.
6. Run full validation gates above.
7. Publish through established maintainer release process only when explicitly instructed.
8. Post-publish: move CHANGELOG `1.3.0 — Unreleased` to dated section; update ROADMAP “Released recently”.

---

## Rollback notes

- v1.3.0 is **additive** relative to v1.2.0.
- Correlation fields, redaction profiles, and streaming options are opt-in.
- Consumers on v1.2.0 unaffected unless they adopt new options.
