# v3.5.1 adoption polish — release readiness

## Summary

Docs-only patch: root README rewrite, package READMEs for all 15 public scoped packages, npm `files` hygiene, adoption scenario docs, broken-agent starter, static SVG assets. **No runtime feature changes.**

## Packages needing patch release

All 16 linked packages (README and/or root `files` changes):

- `agent-inspect`
- `@agent-inspect/ai-sdk`, `@agent-inspect/openai-agents`, `@agent-inspect/langchain`
- `@agent-inspect/harness`, `@agent-inspect/redact`, `@agent-inspect/eval`
- `@agent-inspect/vitest`, `@agent-inspect/jest`, `@agent-inspect/mcp`
- `@agent-inspect/guardrails`, `@agent-inspect/circuit`, `@agent-inspect/viewer`
- `@agent-inspect/mcp-server`, `@agent-inspect/adapter-sdk`, `@agent-inspect/tui`

## Package README status

| Package | README |
| ------- | ------ |
| All public scoped packages | Created/updated; added to `files` |
| `agent-inspect-vscode` | In-repo README only (ignored in changeset) |

## Package files status

- Root `package.json` `files`: adoption docs + hero SVGs added
- Scoped packages: `README.md` in `files` array; `homepage` → package directory

## npm rendering risk

- Root README relative links target files included in tarball or GitHub-only examples
- Package READMEs use absolute GitHub links for docs outside package tarball
- Hero SVG embedded from `docs/assets/` (included in root tarball)

## Validation checklist

- [ ] `pnpm install --frozen-lockfile`
- [ ] `pnpm build`
- [ ] `pnpm typecheck`
- [ ] `pnpm test`
- [ ] `pnpm fixtures:check`
- [ ] `pnpm recipes:check`
- [ ] `pnpm pack:smoke`
- [ ] `pnpm compat:smoke`
- [ ] `npm pack --dry-run`

## Out of scope

- Runtime/API changes
- New dependencies
- Schema changes
- Network/telemetry behavior
- VS Code Marketplace publish

## Publication

Changeset patch → Version Packages PR → merge when CI green → monitor publish workflow.
