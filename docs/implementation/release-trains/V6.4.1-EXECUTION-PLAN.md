# V6.4.1 — Critical Trust and Security

**Train:** pre-v7-stabilization / v6.4.1  
**Theme:** Remove first-use and safety misrepresentation bugs. No schema changes.  
**Authority:** [../ROADMAP-V6.4-TO-PRE-V7.md](../ROADMAP-V6.4-TO-PRE-V7.md) (§ v6.4.1)

## Release goal

Patch release fixing MCP safety boundary, bundle path containment, viewer/Studio XSS, strict plugin manifests, gate validation, Studio init, adversarial fixtures, and public truth alignment.

## Non-goals

- No trace schema change
- No Studio product expansion
- No suite semantics change (v6.5.0)
- No new default network behavior

## Ordered chunks

| Chunk | Deliverable |
|-------|-------------|
| 6.4.1-0 | Baseline audit + threat model + train activation |
| 6.4.1-1 | Fix demo + packed five-minute E2E; fix `verify-safe` README |
| 6.4.1-2 | MCP `prepareMcpToolResult` boundary (redaction, byte caps) |
| 6.4.1-3 | Real bundle safety assessment in MCP (no hard-coded SAFE) |
| 6.4.1-4 | Safe bundle artifact paths and run ID sanitization |
| 6.4.1-5 | Viewer and Studio HTML escaping hardening |
| 6.4.1-6 | Strict plugin manifest boolean/enum parsing |
| 6.4.1-7 | Gate/check numeric range validation |
| 6.4.1-8 | Studio mkdir await + Postgres capability messaging |
| 6.4.1-9 | Adversarial security fixture corpus |
| 6.4.1-10 | Public version/support claims alignment |
| 6.4.1-11 | Release readiness + changeset patch |

## Validation gate

```bash
pnpm build && pnpm typecheck && pnpm test && pnpm test:coverage && pnpm size && pnpm test:all && pnpm fixtures:check && pnpm pack:smoke && git diff --check
```

Focused: `@agent-inspect/mcp-server`, `viewer`, `studio`, `adapter-sdk` tests.

## Release gate

Add: `pnpm recipes:check`, `pnpm compat:smoke`, `pnpm website:typecheck`, `pnpm website:build`, changeset, Version Packages, publish verify.
