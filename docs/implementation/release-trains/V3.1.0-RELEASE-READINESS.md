# v3.1.0 Release Readiness

**Date:** 2026-06-29  
**Status:** implementation complete locally; **pending** `@agent-inspect/harness` first npm publication before Version Packages PR.

## Scope

- Public `@agent-inspect/harness` (package ready; manual first publish required)
- `agent-inspect init` / `agent-inspect doctor`
- Adoption starters under `examples/starters/`
- Onboarding doc updates

## Validation evidence

| Command | Result |
| ------- | ------ |
| `pnpm build` | passed |
| `pnpm typecheck` | passed |
| `pnpm test` | passed (1217 tests) |
| `pnpm recipes:check` | passed |
| `pnpm pack:smoke` | passed (incl. harness tarball) |

## Manual gate

Publish `@agent-inspect/harness@3.0.0` (bootstrap) before merging Version Packages PR that links harness at `3.1.0`.

## Next

1. Maintainer: push local commits + publish harness on npm
2. Create changeset for v3.1.0 minor (linked packages + harness)
3. Version Packages PR → merge → verify publish
