# V4.3.0 release readiness

**Train:** v4.3.0 — Shareable Trace Bundles  
**Target:** `agent-inspect@4.3.0` (all workspace packages minor bump)

## Scope shipped

- RFC: [SHAREABLE-BUNDLES-V4.3.md](../proposals/SHAREABLE-BUNDLES-V4.3.md)
- Core bundle helpers on `@agent-inspect/core/advanced` (`resolveBundleRunIds`, manifest/summary builders, safety aggregation)
- CLI `agent-inspect bundle` with `--session`, `--since`, `--profile`, `--out`, `--allow-unsafe`, `--json`
- Offline HTML + markdown + JSON sidecars; folder output (`.zip` suffix stripped)
- Docs: [BUNDLES.md](../BUNDLES.md), [CLI.md §6.24](../CLI.md#624-bundle)
- Recipe: [shareable-bundle-basic](../../examples/recipes/shareable-bundle-basic/README.md)

## Safety checklist

- [x] Redaction before export (`share` default)
- [x] verify-safe enforcement; `--allow-unsafe` override
- [x] Source traces not mutated (mtime check + read-only reads)
- [x] Path traversal guard for workspace-relative `--out`
- [x] No network I/O in bundle path
- [x] No secrets in fixtures/recipe

## Validation gate

```bash
pnpm build
pnpm typecheck
pnpm test
pnpm test:coverage
pnpm fixtures:check
pnpm recipes:check
pnpm pack:smoke
pnpm compat:smoke
git diff --check
```

## Compatibility

- Additive CLI command and advanced exports
- No schema version change
- v0.1 / v0.2 traces remain readable

## Publish

- Changesets minor bump → `Version Packages` commit → push `main` → `workflow_dispatch` publish (npm@11.18.0 OIDC pin)
