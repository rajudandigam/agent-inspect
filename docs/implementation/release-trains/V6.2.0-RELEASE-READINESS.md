# V6.2.0 release readiness

**Train:** v6.2.0 — Plugin Convention  
**Target:** `agent-inspect@6.2.0` + linked packages @ `6.2.0`

## Scope shipped

- RFC: [PLUGIN-CONVENTION-V6.2.md](../../proposals/PLUGIN-CONVENTION-V6.2.md)
- Plugin manifest schema (`agent-inspect-plugin.manifest.json`) + validator in `@agent-inspect/adapter-sdk`
- Adapter SDK conformance + privacy checklist (existing; wired through manifest validation)
- CLI: `agent-inspect plugins list|doctor|validate`
- Docs: [PLUGINS.md](../../PLUGINS.md)
- Fixture: `fixtures/plugins/valid-manifest.json`

## Product boundary confirmation

- Explicit install only; no hosted marketplace
- No automatic remote code loading from CLI
- Privacy checklist enforced for adapter manifests
- Unsafe network/upload flags warn in `plugins doctor`
- No trace schema changes

## Validation (release gate)

```bash
pnpm build && pnpm typecheck && pnpm test && pnpm test:coverage
pnpm fixtures:check && pnpm recipes:check && pnpm size
pnpm test:all && pnpm pack:smoke && pnpm compat:smoke
git diff --check
```

## Manual gates

- Dynamic code-loading behavior — **not added** (explicit install + manifest validation only)
