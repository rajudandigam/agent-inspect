# v3.5.3 adoption hygiene — release readiness

## Summary

Docs-only patch: remove unavailable README hero SVG from npm tarball, consolidate duplicate scenario/quickstart docs, archive stale public and maintainer docs, lean `docs/README.md` index.

## Changes

- Removed `docs/assets/*.svg` from root package `files` and deleted SVG files (hero pending re-add)
- Deleted `REAL-WORLD-SCENARIOS.md`, `ONE-PAGE-QUICKSTART.md` (merged into USE-CASES / FIRST-TRACE)
- Moved IDE-SURFACES, COMMUNITY-EXTENSION-REGISTRY, HARNESS full docs to `docs/archive/public/` with stubs
- Moved superseded implementation prompts/roadmaps to `docs/archive/implementation/`
- Added `docs/archive/README.md` and `HISTORICAL-REFERENCE.md`

## Validation

- `pnpm typecheck`, `pnpm test`, `git diff --check`
- No runtime changes

## Publication

Changeset patch → Version Packages PR → merge when CI green.
