# v2.5.0 Release Readiness

**Date:** 2026-06-04  
**Starting commit:** `c04cadc` (`feat: integrate guardrails with checks and eval`)  
**Status:** local release-readiness gate passed; ready for release prep.

## Scope

v2.5.0 is the guardrails and circuit breakers train:

- `@agent-inspect/guardrails` — deterministic phrase, PII (via redact), tool-arg, injection, schema, and size rules;
- `@agent-inspect/circuit` — trace analyzers for repetition, retries, timeouts, LLM loops, branch width;
- `check --guardrails` / `check --circuit` CLI flags;
- eval factories `createEvalGuardrailRule` / `createEvalCircuitRule`;
- recipes `guardrails-basic` and `circuit-breaker-basic`.

Out of scope: remote policy engine, LLM judge, compliance certification, automatic remediation.

## Package And Release Strategy

No version bump in this readiness chunk. Release prep adds `@agent-inspect/guardrails` and `@agent-inspect/circuit` to the linked minor changeset (12 public packages).

| Package | Local state | Notes |
| ------- | ----------- | ----- |
| `agent-inspect` | `2.4.0` | CLI check flags |
| `@agent-inspect/guardrails` | `2.4.0`, new | First publish in v2.5 |
| `@agent-inspect/circuit` | `2.4.0`, new | First publish in v2.5 |
| Linked adapters/reporters | `2.4.0` | Minor bump with train |

## Safety

- Guardrails/circuits are advisory local utilities, not compliance tools.
- No default enforcement; no network I/O in packages.
- PII guardrails reuse `@agent-inspect/redact` limitations.

## Release-Readiness Validation Evidence

| Command | Result |
| ------- | ------ |
| `pnpm build` | passed |
| `pnpm test` | passed (guardrails, circuit, cli safety-extensions, eval) |
| `pnpm recipes:check` | passed |

## Readiness Decision

Implementation complete. Version Packages PR [#48](https://github.com/rajudandigam/agent-inspect/pull/48) merged as `44b3dcb`.

## Publication Follow-Up

**Publication complete (2026-06-29):** Publish workflow succeeded for ten linked packages at `2.5.0` on npm (`latest`), including `agent-inspect@2.5.0`.

**Pending bootstrap (same pattern as v2.4 `@agent-inspect/mcp`):**

- `@agent-inspect/guardrails@2.5.0`
- `@agent-inspect/circuit@2.5.0`

Maintainer: first-publish both scoped packages, add Trusted Publisher entries, re-run Publish workflow.
