# v2.5.0 Execution Plan

## Train

```yaml
train: "v2.5.0"
theme: "Guardrails and circuit breakers"
baselineVersion: "previous published train"
executionMode: "codex-autonomous-release-train"
```

## Goal

Add deterministic local safety utilities for repeated agent safety problems, built on checks/eval/redact.

## Product boundaries

This train must preserve:

- local-first behavior;
- no default network calls;
- no root/core framework or provider dependencies;
- no raw chain-of-thought capture;
- no provider pricing engine;
- no hosted service or dashboard;
- deterministic behavior by default;
- old trace readability;
- package ESM/CJS/declaration compatibility.

## Ordered chunks

### Chunk 0 — guardrail/circuit RFC and safety boundary

**Goal:** Define deterministic local guardrail/circuit contracts.

**In scope**
- guardrail result model
- circuit result model
- trace event emission
- check/eval integration
- non-compliance language

**Out of scope**
- remote policy engine
- LLM judge
- compliance certification
- automatic remediation

**Likely files**
- `docs/proposals/GUARDRAILS-AND-CIRCUITS.md`
- `docs/SAFE-TRACE-SHARING.md`
- `docs/LIMITATIONS.md`

**Tests / fixtures**
- docs gate

**Success criteria**
- no compliance claims
- deterministic-first design

**Suggested commit**

```text
docs: define guardrail and circuit contracts
```

### Chunk 1 — @agent-inspect/guardrails scaffold

**Goal:** Add local deterministic guardrail package.

**In scope**
- banned phrase
- PII leak via redact findings
- unsafe tool arguments
- prompt-injection patterns
- structured output violation
- oversize output
- required JSON shape

**Out of scope**
- LLM safety judge
- remote policies
- provider SDKs

**Likely files**
- `packages/guardrails/package.json`
- `packages/guardrails/src/`
- `packages/guardrails/test/`
- `tsup.guardrails.config.ts`

**Tests / fixtures**
- unit tests
- PII fixture tests
- package smoke

**Success criteria**
- guardrails deterministic
- redact package reused
- no root dependency impact

**Suggested commit**

```text
feat(guardrails): add deterministic guardrail package
```

### Chunk 2 — @agent-inspect/circuit scaffold

**Goal:** Add local circuit-breaker utilities for loop/retry/tool-call patterns.

**In scope**
- same tool repetition
- same args repetition
- max loop iterations
- max retries
- long tool timeout
- runaway LLM loop
- excessive branch width

**Out of scope**
- production APM
- automatic kill switch without user config
- framework monkey patching

**Likely files**
- `packages/circuit/package.json`
- `packages/circuit/src/`
- `packages/circuit/test/`
- `tsup.circuit.config.ts`

**Tests / fixtures**
- unit tests
- trace fixture tests
- package smoke

**Success criteria**
- circuit utilities return results/evidence
- no application mutation by default

**Suggested commit**

```text
feat(circuit): add local circuit utilities
```

### Chunk 3 — check/eval integration

**Goal:** Expose guardrail/circuit results through existing check/eval workflows.

**In scope**
- check rule adapters
- eval checks
- CLI flags/config
- evidence linking
- trace event output where configured

**Out of scope**
- default enforcement
- remote reporting

**Likely files**
- `packages/core/src/checks/`
- `packages/eval/src/`
- `packages/cli/src/check.ts`
- `docs/CLI.md`

**Tests / fixtures**
- check/eval integration tests
- fixtures

**Success criteria**
- guardrails feed into check/eval
- exit codes documented
- evidence useful

**Suggested commit**

```text
feat: integrate guardrails with checks and eval
```

### Chunk 4 — recipes and release readiness

**Goal:** Document safe deterministic safety workflows.

**In scope**
- recipes
- README examples
- no-compliance claims
- readiness

**Out of scope**
- version/publish before authorization

**Likely files**
- `examples/recipes/guardrails-basic/`
- `examples/recipes/circuit-breaker-basic/`
- `docs/SAFE-TRACE-SHARING.md`
- `docs/implementation/release-trains/V2.5.0-RELEASE-READINESS.md`

**Tests / fixtures**
- recipes check
- full release gate

**Success criteria**
- examples show safe local use
- package boundaries pass

**Suggested commit**

```text
docs: prepare v2.5 release readiness
```


## Train validation

Run the appropriate focused gates per chunk. Before release readiness, run:

```bash
pnpm install --frozen-lockfile
pnpm build
pnpm typecheck
pnpm test
pnpm test:coverage
pnpm fixtures:check
pnpm recipes:check
pnpm size
pnpm test:all
pnpm pack:smoke
pnpm compat:smoke
npm pack --dry-run
git diff --check
```

Additional train-specific validation:

- deterministic safety fixtures;
- no remote provider or policy engine tests;
- package smoke for new packages.

## Release readiness

Create/update:

```text
docs/implementation/release-trains/v2.5.0-RELEASE-READINESS.md
```

Release notes should cover:

- guardrail/circuit package behavior;
- check/eval integration;
- safety disclaimers;
- known limitations.

Do not publish until maintainer explicitly authorizes release prep.
