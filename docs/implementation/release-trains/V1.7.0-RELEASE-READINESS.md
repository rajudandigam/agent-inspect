# v1.7.0 Release Readiness

**Date:** 2026-06-25  
**Starting commit:** `ce92e2b` (`test: add adapter conformance fixture matrix`)  
**Status:** release PR pending through Changesets automation

## Scope

v1.7.0 builds on the v1.6 runtime/reader/writer foundation with framework-native adapter adoption:

- experimental `@agent-inspect/ai-sdk` package for Vercel AI SDK v6 telemetry integrations;
- metadata-only AI SDK run, step, tool, error, streaming, and token-usage mapping;
- local no-network AI SDK recipe coverage;
- OpenAI Agents JS tracing processor RFC and scaffold-only package boundary;
- LangGraph support decision through `@agent-inspect/langchain`;
- adapter conformance matrix for local-first defaults and fixture expectations.

## Package And Release Strategy

The repository publish workflow runs `changesets/action@v1` with `publish: pnpm run release`. On pushes to `main`, the workflow first runs release-train gates, then either creates a Version Packages PR when changesets exist or publishes when versioned package files are merged.

Release preparation in this chunk:

- Added a Changeset for a linked minor release of `agent-inspect`, `@agent-inspect/langchain`, `@agent-inspect/tui`, and `@agent-inspect/ai-sdk`.
- Made `@agent-inspect/ai-sdk` publishable with `publishConfig.access: "public"`.
- Kept `@agent-inspect/openai-agents` private and scaffold-only because runtime mapping is not implemented.
- Did not run local publish, create tags, create releases, or change version fields directly.

Expected Version Packages PR effect:

| Package | Expected v1.7 state |
| ------- | ------------------- |
| `agent-inspect` | versioned to `1.7.0` and published by CI |
| `@agent-inspect/langchain` | linked versioned to `1.7.0` and published by CI |
| `@agent-inspect/tui` | linked versioned to `1.7.0` and published by CI |
| `@agent-inspect/ai-sdk` | first public release at `1.7.0` by CI |
| `@agent-inspect/openai-agents` | private, unpublished scaffold |

## Compatibility

- Stable root imports remain unchanged.
- Manual traces still write `schemaVersion: "0.1"`.
- v0.1 and v0.2 AgentInspect JSONL traces remain readable.
- v0.2 adapter events remain experimental and additive.
- Root/core packages do not depend on AI SDK, OpenAI Agents, LangGraph, OpenTelemetry, or LangChain.
- Node support remains `>=20`.

## Security And Network

- No default telemetry upload, vendor sink, hosted ingestion, collector client, or replay behavior is added.
- AI SDK examples require `recordInputs: false` and `recordOutputs: false`.
- AI SDK capture defaults remain metadata-only; prompts, messages, generated text, stream chunks, tool inputs/outputs, headers, request bodies, and response bodies are not persisted by default.
- OpenAI Agents scaffold does not install itself globally, perform network I/O, or preserve default backend export behavior by default.
- LangGraph support remains through explicit LangChain callback boundaries unless future fixtures prove a gap.

## Registry Evidence Before Release PR

| Check | Result | Evidence |
| ----- | ------ | -------- |
| `npm view agent-inspect version` | passed | `1.6.0` |
| `npm view @agent-inspect/langchain version` | passed | `1.6.0` |
| `npm view @agent-inspect/tui version` | passed | `1.6.0` |
| `npm view @agent-inspect/ai-sdk version` | expected 404 | Package is not yet published; v1.7.0 should be its first public release. |

## Validation Evidence

| Command | Result | Evidence |
| ------- | ------ | -------- |
| `pnpm exec vitest run packages/core/test/package-smoke.test.ts` | passed | 1 file, 7 tests. |
| `pnpm build` | passed | Core, CLI, LangChain, TUI, AI SDK, and OpenAI Agents bundles/types built. Non-fatal tsup warnings about unused bundled `fs/promises` imports remain. |
| `pnpm typecheck` | passed | `tsc --noEmit`. |
| `pnpm test` | passed | 113 files, 998 tests. |
| `pnpm test:coverage` | passed | 113 files, 998 tests; all-files coverage: statements 81.16%, branches 80.32%, functions 95.3%, lines 81.16%. |
| `pnpm size` | passed | Size limit 120 kB; measured 38.77 kB with all dependencies, minified and brotlied. |
| `pnpm test:all` | passed | Re-ran typecheck, test, build, and size successfully; test phase again passed 113 files / 998 tests; size 38.77 kB. |
| `pnpm fixtures:check` | passed | 9 v0.1 JSONL files, 6 v0.2 JSONL files, 8 logs, 5 configs. |
| `pnpm recipes:check` | passed | 14 recipes validated, including AI SDK local telemetry. |
| `pnpm pack:smoke` | passed | Tarball install, ESM import, CJS require, CLI 1.6.0, local bin, npm exec, and `npx --no-install --help`. |
| `pnpm compat:smoke` | passed | Bundled CJS, ESM/CJS consumers, subpath ESM/CJS, Jest-style CJS pattern, ts-jest Node16 compile, and CLI help. |
| `npm pack --dry-run` | passed | Dry-run tarball `agent-inspect-1.6.0.tgz`, 118 files, 1.1 MB package size, 5.6 MB unpacked. |

## Remaining Release Steps

1. Push this readiness and changeset commit to `main`.
2. Let the Publish workflow create the v1.7 Version Packages PR.
3. Validate that PR's CI and package metadata.
4. Merge the Version Packages PR only if it bumps the intended public package set and keeps `@agent-inspect/openai-agents` private.
5. Confirm the post-merge Publish workflow publishes v1.7.0 through npm Trusted Publishing or configured automation.
