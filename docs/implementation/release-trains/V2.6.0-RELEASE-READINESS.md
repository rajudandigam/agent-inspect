# v2.6.0 Release Readiness

**Date:** 2026-06-04  
**Starting commit:** `688857c` (`feat(mcp-server): add read-only trace tools`)  
**Status:** local release-readiness gate passed; ready for release prep.

## Scope

v2.6.0 is the optional surfaces train:

- `@agent-inspect/viewer` — localhost read-only HTTP viewer;
- `agent-inspect serve` CLI command;
- `@agent-inspect/mcp-server` — read-only MCP tools over local traces;
- IDE extension **deferred** ([IDE-SURFACES.md](../../IDE-SURFACES.md));
- recipes: `read-only-mcp-server`.

Out of scope: cloud viewer, IDE marketplace extension, trace mutation, MCP tool invocation, replay.

## Package And Release Strategy

No version bump in this readiness chunk. Release prep adds `@agent-inspect/viewer` and `@agent-inspect/mcp-server` to the linked minor changeset (14 public packages).

| Package | Local state | Notes |
| ------- | ----------- | ----- |
| `agent-inspect` | `2.5.0` | `serve` command |
| `@agent-inspect/viewer` | `2.5.0`, new | First publish in v2.6 |
| `@agent-inspect/mcp-server` | `2.5.0`, new | First publish in v2.6 |
| Linked adapters/reporters | `2.5.0` | Minor bump with train |

**Pending from v2.5:** first-publish `@agent-inspect/guardrails` and `@agent-inspect/circuit` if not yet on npm.

## Safety

- Viewer binds `127.0.0.1` by default; warns on `0.0.0.0`.
- MCP server tools are read-only; default redaction profile `share`.
- No new root/core dependencies for optional surfaces.
- IDE extension deferred until demand gate passes.

## Release-Readiness Validation Evidence

| Command | Result |
| ------- | ------ |
| `pnpm build` | passed |
| `pnpm typecheck` | passed |
| `pnpm test` | passed (1198 tests) |
| `pnpm recipes:check` | passed (28 recipes) |
| `pnpm pack:smoke` | passed (viewer + mcp-server) |

## Readiness Decision

Implementation and documentation complete on `main` through chunk 4. Proceed to release prep (changeset, Version Packages PR) when maintainer authorizes.

## Publication Follow-Up

After merge of Version Packages PR:

1. Verify publish workflow for linked packages at `2.6.0`.
2. First-publish `@agent-inspect/viewer` and `@agent-inspect/mcp-server` if npm returns E404 (Trusted Publisher pattern).
3. Complete any remaining v2.5 bootstrap (`guardrails`, `circuit`).
