# v2.4.0 Execution Plan

## Train

```yaml
train: "v2.4.0"
theme: "Sessions, multi-agent, MCP, and workflow causality"
baselineVersion: "previous published train"
executionMode: "codex-autonomous-release-train"
```

## Goal

Support real multi-run agent systems through session navigation, handoffs, retries, and initial MCP tool-call tracing.

## Product boundaries

This train must preserve:

- local-first behavior;
- no default network calls;
- no root/core framework or provider dependencies;
- no raw chain-of-thought capture;
- no provider pricing engine;
- no hosted service or dashboard;
- deterministic behavior by default;
- old trace readability;
- package ESM/CJS/declaration compatibility.

## Ordered chunks

### Chunk 0 — session model RFC and compatibility plan

**Goal:** Define additive session/group/attempt fields and relationship rules.

**In scope**
- sessionId
- conversationId
- groupId
- parentGroupId
- attempt
- handoff
- sub-agent
- job/queue/workflow metadata
- MCP semantic boundary

**Out of scope**
- schema-breaking rewrite
- MCP server
- viewer

**Likely files**
- `docs/proposals/SESSIONS-AND-WORKFLOW-CAUSALITY.md`
- `docs/SCHEMA.md`
- `docs/implementation/release-trains/V2.4.0-EXECUTION-PLAN.md`

**Tests / fixtures**
- docs gate

**Success criteria**
- additive model only
- ambiguity policy documented

**Suggested commit**

```text
docs: define session and workflow causality model
```

### Chunk 1 — session-aware reader and index helpers

**Goal:** Build reusable session/group aggregation helpers.

**In scope**
- buildSessionIndex
- group runs
- attempt/retry grouping
- handoff edges
- critical path model
- warnings

**Out of scope**
- CLI UI polish
- database/index storage

**Likely files**
- `packages/core/src/sessions/`
- `packages/core/test/sessions/`
- `fixtures/sessions/`

**Tests / fixtures**
- session fixtures
- mixed source fixtures
- large fixture sanity tests

**Success criteria**
- sessions are deterministic
- no timestamp-only invented causality
- warnings for ambiguity

**Suggested commit**

```text
feat: add session aggregation helpers
```

### Chunk 2 — sessions/session CLI

**Goal:** Add `agent-inspect sessions` and `agent-inspect session` read-only commands.

**In scope**
- sessions list
- session view
- timeline
- critical path
- JSON output
- diagnostics

**Out of scope**
- trace mutation
- viewer
- MCP server

**Likely files**
- `packages/cli/src/sessions.ts`
- `packages/cli/test/sessions.test.ts`
- `docs/CLI.md`

**Tests / fixtures**
- CLI tests
- fixture tests
- JSON stable snapshots

**Success criteria**
- one multi-agent/session trace is browsable
- handoffs/retries clear

**Suggested commit**

```text
feat: add session inspection commands
```

### Chunk 3 — session-aware search/cohort/checks

**Goal:** Allow local checks/search to operate by session/group.

**In scope**
- search --session
- check group/session input
- cohort group-by session/group
- evidence references

**Out of scope**
- full eval dataset platform
- LLM judge

**Likely files**
- `packages/core/src/search.ts`
- `packages/core/src/checks/`
- `packages/cli/src/search.ts`
- `packages/cli/src/check.ts`

**Tests / fixtures**
- search/check fixtures
- group evidence tests

**Success criteria**
- group-level checks produce actionable evidence
- old search behavior unchanged

**Suggested commit**

```text
feat: add session-aware search and checks
```

### Chunk 4 — MCP telemetry package or subpath

**Goal:** Trace MCP client calls without building a gateway product.

**In scope**
- client connection
- server identity
- tools/list
- tools/call
- tool name
- arguments/result summaries
- duration
- errors
- session metadata

**Out of scope**
- read-only MCP server
- gateway
- tool invocation server
- automatic remediation

**Likely files**
- `packages/mcp/package.json`
- `packages/mcp/src/`
- `packages/mcp/test/`
- `tsup.mcp.config.ts`
- `examples/recipes/mcp-client-tracing/`

**Tests / fixtures**
- mock MCP client fixtures
- no-network tests
- package smoke if public

**Success criteria**
- MCP tool calls appear as tool spans with source metadata
- no gateway behavior

**Suggested commit**

```text
feat(mcp): add local MCP tool-call tracing
```

### Chunk 5 — docs, recipes, and v2.4 release readiness

**Goal:** Document multi-agent/session/MCP workflows and verify train readiness.

**In scope**
- README
- CLI
- ADAPTERS
- SCHEMA
- recipes
- readiness

**Out of scope**
- viewer
- MCP server

**Likely files**
- `README.md`
- `docs/CLI.md`
- `docs/SCHEMA.md`
- `docs/ADAPTERS.md`
- `examples/recipes/README.md`
- `docs/implementation/release-trains/V2.4.0-RELEASE-READINESS.md`

**Tests / fixtures**
- full release gate
- fixtures/recipes

**Success criteria**
- session workflow is understandable
- MCP scope boundaries clear

**Suggested commit**

```text
docs: prepare v2.4 release readiness
```


## Train validation

Run the appropriate focused gates per chunk. Before release readiness, run:

```bash
pnpm install --frozen-lockfile
pnpm build
pnpm typecheck
pnpm test
pnpm test:coverage
pnpm fixtures:check
pnpm recipes:check
pnpm size
pnpm test:all
pnpm pack:smoke
pnpm compat:smoke
npm pack --dry-run
git diff --check
```

Additional train-specific validation:

- session/multi-run fixtures;
- MCP mocked fixtures;
- no timestamp-only structure fabrication;
- no MCP gateway behavior.

## Release readiness

Create/update:

```text
docs/implementation/release-trains/v2.4.0-RELEASE-READINESS.md
```

Release notes should cover:

- sessions commands;
- MCP telemetry package/subpath;
- compatibility and limitations;
- no gateway/non-mutating guarantees.

Do not publish until maintainer explicitly authorizes release prep.
