# V6.0.0 release readiness

**Train:** v6.0.0 — Self-hosted Studio  
**Target:** `agent-inspect@6.0.0` + linked packages @ `6.0.0` (18 incl. new `@agent-inspect/studio`)

## Scope shipped

- RFC: [SELF-HOSTED-STUDIO-V6.0.md](../proposals/SELF-HOSTED-STUDIO-V6.0.md)
- New optional package `@agent-inspect/studio` with localhost read-only multi-project analyzer
- `agent-inspect studio` CLI (optional install)
- Studio registry import, SQLite cache, project views (runs/sessions/suites/checks/search/diff/reports/observations/guardrails/redaction/bundle hints)
- Optional basic auth (`--auth basic --password-env`)
- Self-hosting docs: [SELF-HOSTING.md](../../SELF-HOSTING.md)
- Fixture: `fixtures/studio/`

## Manual gate (maintainer)

- **First publication:** `@agent-inspect/studio` requires npm Trusted Publishing setup before CI can publish the new package. Other linked packages can publish to `6.0.0` as usual.

## Product boundary confirmation

- Self-hosted only; localhost by default
- Read-only GET routes; no default upload
- SQLite driver scoped to `@agent-inspect/studio` only
- No Postgres implementation in v6.0.0 (explicit error with guidance)
