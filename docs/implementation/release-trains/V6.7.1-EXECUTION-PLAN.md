# V6.7.1 — Corrective Patch (Release Tooling + Public Truth)

**Train:** `pre-v7-completion-from-current-6.7`  
**Theme:** Durable linked-package versioning and accurate public presentation.  
**Authority:** [../reviews/V6.7.0-PRE-V7-RECONCILIATION-AUDIT.md](../reviews/V6.7.0-PRE-V7-RECONCILIATION-AUDIT.md)

## Justification

6.7.0 is the technical launch candidate. 6.7.1 is **not** semver bookkeeping — it fixes:

1. Recurring manual alignment of `@agent-inspect/redact`, `@agent-inspect/guardrails`, `@agent-inspect/circuit` on every Version Packages run.
2. Stale public version claims (README at 6.4.0 vs npm 6.7.0).

Out of scope for 6.7.1 unless a security regression is found: TraceContract depth expansion, Studio HTML productization, full golden-path automation.

## Ordered chunks

```text
6.7.1-C7a  Changesets fixed group + check-linked-versions.mjs + CI/publish gate
6.7.1-C7b  README, ROADMAP, adoption evidence accuracy, MCP server version string if stale
6.7.1-R    Release readiness + patch changeset + Version Packages + publish verify
```

## 6.7.1-C7a — Linked-package release fix

**Files:**

- `.changeset/config.json` — move 18-package array from `linked` to `fixed`
- `scripts/check-linked-versions.mjs` — fail when fixed-group versions diverge
- `package.json` — `linked-versions:check` script; wire into `test:all` / `prepublish:checks`
- `.github/workflows/publish.yml` — run check in verify step
- `.github/workflows/ci.yml` — run check on PRs

**Verification:**

```bash
pnpm run linked-versions:check
pnpm run pack:smoke
```

## 6.7.1-C7b — Public truth

**Files:**

- `README.md` — current release 6.7.0, package count, three-workflow lead
- `ROADMAP.md` — pre-v7 current/freeze framing; demote stale v2 “Next”
- `docs/implementation/PRE-V7-ADOPTION-EVIDENCE.md` — accurate pending status
- `packages/mcp-server/src/index.ts` — server version from package metadata if drifted

**Verification:**

```bash
pnpm typecheck
pnpm test
git diff --check
```

## Release readiness

Create `V6.7.1-RELEASE-READINESS.md` before changeset. Full gate per audit Phase H.

## Skip condition

Skip 6.7.1 entirely if C7a and C7b are already on `main` and npm aligned — not the case as of audit date.
