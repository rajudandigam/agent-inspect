# V6.3.0 release readiness

**Train:** v6.3.0 — MCP Coding-agent Workflows  
**Target:** `agent-inspect@6.3.0` + `@agent-inspect/mcp-server@6.3.0`

## Scope shipped

- RFC: [MCP-WORKFLOW-V6.3.md](../../proposals/MCP-WORKFLOW-V6.3.md)
- MCP tools: `summarize_failed_run`, `retrieve_decision_notes`, `find_failed_observation`, `create_share_safe_bundle`
- Existing read-only scaffold + 8 base tools (v2.6)
- Docs: [MCP.md](../../MCP.md)
- Recipe: `examples/recipes/read-only-mcp-server/`

## Product boundary confirmation

- Read-only MCP server; no code edits or auto-fix
- Share redaction default on exports
- No hidden network beyond stdio transport
- No trace schema changes

## Validation (release gate)

```bash
pnpm build && pnpm typecheck && pnpm test && pnpm test:coverage
pnpm fixtures:check && pnpm recipes:check && pnpm size
pnpm test:all && pnpm pack:smoke && pnpm compat:smoke
git diff --check
```

## Manual gates

- MCP transport review — stdio only, no remote fetch (verified)
- `@agent-inspect/mcp-server` already published; minor bump only
