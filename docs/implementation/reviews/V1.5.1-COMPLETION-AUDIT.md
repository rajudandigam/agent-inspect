# v1.5.1 completion audit

**Audit date:** 2026-06-24
**Branch:** `main`
**HEAD:** `44e3843` (`added roadmap`)
**Published baseline:** `agent-inspect@1.5.0`, `@agent-inspect/langchain@1.5.0`, `@agent-inspect/tui@1.5.0`
**Decision:** v1.5.1 is incomplete; do not start v1.6
**Current stop:** MANUAL GATE 1 — v1.5.1 completion review

## Executive summary

The worktree is clean and v1.5.1 Chunk 1 is committed at `633d89b`. The built CLI reports the public package version, and list/stats/search now use correct v0.1/v0.2 metadata and exact file paths.

Chunks 2–4 remain unimplemented. Current HEAD still reproduces complete-report redaction leaks, mixed-format reordering, incorrect error-name-to-stack conversion, loss of token `total`, absence of cached-token vocabulary, stale v1.5.0/v1.6 documentation, and missing v1.5.1 release readiness. Package versions remain at 1.5.0 and there is no pending changeset.

The next implementation unit is Chunk 2 — report safety and normalization correctness. This audit does not implement it.

## Phase 0 repository snapshot

| Item | Evidence | Status |
|---|---|---|
| Branch and HEAD | `main` at `44e3843`; `origin/main` aligned | complete |
| Worktree | `git status --short` returned no entries | complete |
| Diff hygiene | opening `git diff --check` passed | complete |
| Latest v1.5.0 tag | `agent-inspect@1.5.0` | complete |
| Commits after v1.5.0 | `d613ba0`, `633d89b`, `44e3843` | complete |
| Published version in repository docs | 1.5.0 | complete |
| Public manifest versions | all three public packages remain 1.5.0 | complete |
| Private manifest versions | `@agent-inspect/core@0.1.0`, `@agent-inspect/cli@0.1.0` | complete |
| Version preparation | no package version or lockfile version preparation | missing |
| Pending changeset | `.changeset/` contains only `config.json` | missing |
| v1.5.1 release readiness | no `V1.5.1-RELEASE-READINESS.md` exists | missing |
| Active v1.5.1 implementation | Chunk 1 only | partially complete |
| `AGENTS.md` / `AGENTS.override.md` | absent | intentionally deferred until confirmed v1.5.1 publication under the bootstrap plan |
| Repository Codex instructions | no tracked `.codex/` project instructions found | intentionally deferred until confirmed v1.5.1 publication |

### Commits after the v1.5.0 tag

```text
44e3843 added roadmap
633d89b fix(v1.5.1): correct CLI version and dual-format metadata reads
d613ba0 added follow up changes
```

`d613ba0` added the v1.5.0 post-release review, v1.5.1 patch plan, and planning state. `633d89b` implemented Chunk 1. `44e3843` added the canonical future-roadmap input without changing runtime behavior.

### Files changed after the v1.5.0 tag

- Planning and state: `docs/implementation/RELEASE-TRAIN-STATE.md`, `docs/implementation/release-trains/V1.5.1-PATCH-PLAN.md`, `docs/implementation/reviews/V1.5.0-POST-RELEASE-REVIEW.md`.
- Canonical input: `docs/implementation/CANONICAL-ROADMAP-V1.6-TO-V3.md`.
- Chunk 1 runtime: CLI version wiring plus core trace reading, metadata, stats, and search paths.
- Chunk 1 tests/fixtures: CLI/core parity tests, v0.1/v0.2 parity fixtures, fixture validation, and packed CLI version smoke.
- No package manifest, lockfile, changeset, dependency, default-network, or schema-write change occurred after the tag.

## Package and compatibility inventory

### Public and private packages

| Package | Version | Published/private state |
|---|---:|---|
| `agent-inspect` | 1.5.0 | public |
| `@agent-inspect/langchain` | 1.5.0 | public |
| `@agent-inspect/tui` | 1.5.0 | public |
| `@agent-inspect/core` | 0.1.0 | private |
| `@agent-inspect/cli` | 0.1.0 | private |

Root runtime dependencies remain exactly `chalk`, `commander`, and `nanoid`. Core depends only on `chalk` and `nanoid`; framework and UI dependencies remain isolated in optional packages.

### Root exports

The public root retains `.` plus additive conditional ESM/CJS/type subpaths:

- `agent-inspect/advanced`
- `agent-inspect/persisted`
- `agent-inspect/logs`
- `agent-inspect/exporters`
- `agent-inspect/diff`

No root export was removed and no new subpath was added during v1.5.1 Chunk 1.

### Schema behavior

| Requirement | Evidence | Classification |
|---|---|---|
| Global/manual writer remains v0.1 | `validateEvent` and storage write path require `schemaVersion: "0.1"` | complete |
| v0.1 read | canonical v0.1 fixtures and migration tests remain | complete |
| v0.2 read | `parseTraceJsonl` validates and normalizes persisted rows | complete |
| Embedded run ID differs from filename | parity fixture and exact-path stats/search tests | complete |
| No third persisted model | only v0.1 trace and v0.2 persisted models exist | complete |
| No destructive migration | readers are local and read-only | complete |

## v1.5.1 plan reconciliation

### Chunk status

| Chunk | Scope | Classification | Evidence |
|---|---|---|---|
| 1 | Version and canonical metadata read path | complete | committed at `633d89b`; built/packed CLI version and v0.1/v0.2 parity tests added |
| 2 | Report safety and normalization correctness | missing | current source and built output reproduce all three original faults |
| 3 | Token vocabulary completion | missing | token types/converters remain unchanged from v1.5.0 |
| 4 | Documentation and release readiness | missing | stale docs remain and no v1.5.1 readiness document exists |

## Requirement-by-requirement classification

### CLI version and canonical metadata

| Requirement | Classification | Evidence |
|---|---|---|
| Built CLI version comes from public package version | complete | CLI imports root manifest version; built CLI prints `1.5.0` |
| Packed CLI version is release-gated | complete | package smoke compares installed binary output with root manifest |
| List handles v0.1 and v0.2 metadata | complete | core/CLI tests and parity fixtures |
| Stats handles v0.1 and v0.2 metadata | complete | exact-path parity and correlation tests |
| Search handles v0.1 and v0.2 metadata | complete | run/step parity tests |
| Embedded v0.2 run IDs work when filenames differ | complete | `dual-format-parity.jsonl` fixtures intentionally differ |

### Complete report redaction

| Requirement | Classification | Evidence / risk / required completion |
|---|---|---|
| `share` protects the complete report | missing | `buildRunReport` builds what/timeline from original events and redacts only the tree. Risk: correlation/customer identifiers can leak. Add Markdown/HTML complete-document tests in Chunk 2. |
| `strict` protects the complete report | missing | current strict reproduction retains `corr-secret`, `req-secret`, and `run-error-secret`. Risk: sensitive prompt/message/error data can leak. Redact structured event copies before any renderer. |
| Titles and summaries covered | missing | title/what derive from unredacted values. Add synthetic title/summary sentinel assertions. |
| Timeline covered | missing | timeline derives from original events. Add share/strict timeline sentinel assertions. |
| Errors covered | partially complete | tree export applies existing attribute redaction, but what/run-error content is unredacted. Test run-only and step errors in both formats. |
| Correlation fields covered | missing | what section emits original run metadata. Add `correlationId`, `requestId`, `decisionId`, and `groupId` tests. |
| Tree covered | complete | `redactRunTreeForExport` already protects tree attributes without mutating the source tree. Complete-document integration is still missing. |

Files involved:

- `packages/core/src/report.ts`
- `packages/core/src/exporters/redact-export.ts` or a narrowly scoped structured-event redaction helper
- `packages/cli/src/index.ts`
- `packages/cli/src/report.ts` if option plumbing changes
- `packages/core/test/report.test.ts`
- `packages/core/test/redaction-profiles.test.ts`
- `packages/core/test/security-redaction.test.ts`
- `packages/cli/test/report.test.ts`

### Normalization order and error fidelity

| Requirement | Classification | Evidence / risk / required completion |
|---|---|---|
| Preserve mixed v0.1/v0.2 source order | missing | parser still returns all v0.1 rows before converted v0.2 rows. Risk: misleading chronology and tree construction. Convert valid v0.2 rows inline and assert exact order. |
| Do not fabricate `error.name` as stack | missing | `TypeError` still becomes `stack: "TypeError"`. Risk: corrupted diagnostic evidence. |
| Preserve a real supplied stack | missing | even `attributes.errorStack: "Error: real stack"` is ignored while name is used. Restore only valid `attributes.errorStack`. |
| Existing ordering/error regression tests | missing | mixed test asserts only format/count; converter tests do not distinguish name from stack. |

Files involved:

- `packages/core/src/read-trace.ts`
- `packages/core/src/persisted/to-trace-event.ts`
- `packages/core/test/read-trace.test.ts`
- `packages/core/test/persisted/to-trace-event.test.ts`
- `packages/core/test/fixtures/mixed-v0.1-v0.2-order.jsonl` if a reusable fixture is added

### Token vocabulary

| Requirement | Classification | Evidence / risk / required completion |
|---|---|---|
| Input | complete | present in both token representations and converters |
| Output | complete | present in both token representations and converters |
| Preserve supplied total | partially complete | v0.2 and persisted-to-InspectEvent support total, but v0.2-to-v0.1 normalization drops it |
| Derive total only when absent | partially complete | some v0.1/InspectEvent conversion paths derive input + output; behavior is not consistent across all paths |
| Cached tokens where supplied | missing | neither `TokenMetadata` nor `PersistedTokenUsage` has `cached` |
| No double-counting cached tokens | missing | cannot be tested until cached vocabulary exists |
| No provider cost engine | complete | no pricing tables, provider lookup, or runtime cost calculation exists |
| Preserve optional supplied cost only when approved | not applicable | the active v1.5 vocabulary has no approved canonical cost field; arbitrary attributes remain opaque |

Files involved:

- `packages/core/src/types.ts`
- `packages/core/src/types/persisted-inspect-event.ts`
- all four converter modules under `packages/core/src/persisted/`
- `packages/core/src/trace-metadata.ts`
- `packages/core/src/what.ts`
- token, converter, read, summary, what, and report tests
- paired token fixtures and fixture documentation

### Documentation, regression, and readiness

| Requirement | Classification | Evidence / required completion |
|---|---|---|
| Correct stale v1.5.0 CLI wording | missing | `docs/CLI.md` still describes v0.2 as future and report as v0.1/tree-only |
| Correct v1.5.0 public roadmap state | missing | maintainer table still says v1.5.0 is in progress/planning |
| Correct active maintainer tooling language | missing | active docs still point to Cursor as execution tool |
| Complete v1.5.0 post-publish record | missing | published status exists but verification checkboxes remain unchecked |
| v1.5.1 complete regression gate | missing | only Chunk 1 runtime validation exists |
| v1.5.1 release-readiness document | missing | file does not exist |
| Package versions prepared | missing | public manifests remain 1.5.0 by design pending release authorization |
| Changeset prepared | missing | none exists by design pending release authorization |
| Docs claim v1.5.1 complete/published | not applicable | no active document currently makes that false claim |

Chunk 4 should update only the v1.5.1/v1.5.0 correction and release evidence. The broader canonical-roadmap reset, Codex guide, `AGENTS.md`, and future release sequence remain deferred until the maintainer confirms v1.5.1 publication, as required by the bootstrap plan.

## Roadmap and instruction discrepancies

### Canonical input path

The bootstrap prompt names:

```text
docs/implementation/incoming/CANONICAL-ROADMAP-V1.6-TO-V3.md
```

The committed file is instead:

```text
docs/implementation/CANONICAL-ROADMAP-V1.6-TO-V3.md
```

Because `44e3843` added this file as the current HEAD change and its content matches the supplied canonical positioning and release sequence, this audit treats it as the canonical future-roadmap input. Recommended resolution after v1.5.1 publication: place the durable active roadmap at the paths specified by Phases 2–3 and clearly identify this large input file as source material or archive.

### Active roadmap drift

- `ROADMAP.md` still marks v1.5.0 as in progress/planning and describes v1.6 as AI SDK only.
- `docs/implementation/ROADMAP-EXECUTION-V1.5-TO-V2.md` retains a v1.4 baseline and the superseded future sequence.
- `docs/implementation/CURSOR-MAINTAINER-ROADMAP.md` calls Cursor the active execution tool, calls v1.5.0 current/planning, and assigns AI SDK to v1.6.
- No concise `AGENTS.md` or detailed Codex maintainer guide exists.

These are confirmed discrepancies, but the bootstrap ordering intentionally defers their broad reset until v1.5.1 has been published and verified. They must not be silently treated as the v1.5.1 Chunk 2 scope.

## Security, dependency, and network assessment

- **Security:** Chunk 2 complete-report redaction is the highest-risk missing item. Existing write-time and tree-export redaction tests remain intact.
- **Dependencies:** no root/core dependency was added after v1.5.0. Root remains `chalk`, `commander`, `nanoid`.
- **Network:** no runtime uploader, writer, telemetry, pricing lookup, or default network behavior was added.
- **Capture:** no default full prompt/output or raw chain-of-thought capture was added.
- **Package surface:** no root or subpath export was removed.

## Recommended completion sequence

Do not combine these commits without explicit maintainer approval:

1. **Chunk 2 — report safety and normalization correctness**
   - whole-report redaction;
   - mixed source ordering;
   - error name/stack fidelity;
   - focused security and converter tests.
2. **Chunk 3 — token vocabulary completion**
   - input/output/total/cached consistency;
   - paired fixtures and aggregation tests;
   - no cost engine.
3. **Chunk 4 — documentation and release readiness**
   - v1.5 correction docs;
   - full regression gate;
   - `V1.5.1-RELEASE-READINESS.md`;
   - stop before changeset/version preparation.

## Exact files proposed for the next phase

The next phase is v1.5.1 Chunk 2 only. Expected files:

- `packages/core/src/report.ts`
- `packages/core/src/read-trace.ts`
- `packages/core/src/persisted/to-trace-event.ts`
- `packages/core/src/exporters/redact-export.ts` or one new narrowly scoped redaction helper
- `packages/cli/src/index.ts`
- `packages/cli/src/report.ts` only if required by option plumbing
- `packages/core/test/report.test.ts`
- `packages/core/test/read-trace.test.ts`
- `packages/core/test/persisted/to-trace-event.test.ts`
- `packages/core/test/redaction-profiles.test.ts`
- `packages/core/test/security-redaction.test.ts`
- `packages/cli/test/report.test.ts`
- an optional synthetic mixed-order fixture under `packages/core/test/fixtures/`
- `docs/implementation/RELEASE-TRAIN-STATE.md`

## Manual gate

**MANUAL GATE 1 — v1.5.1 completion review**

Maintainer action required:

1. Review this audit and the revised v1.5.1 patch status.
2. Commit and push the documentation-only audit update.
3. Confirm the commit hash.
4. Explicitly authorize v1.5.1 Chunk 2.

Do not start v1.6, create release versions, add a changeset, or perform roadmap reset work at this gate.

