# v1.5.1 completion audit

**Audit date:** 2026-06-24
**Branch:** `main`
**Current HEAD at audit start:** `181a619` (`fix(v1.5.1): preserve complete token usage vocabulary`)
**Published baseline:** `agent-inspect@1.5.0`, `@agent-inspect/langchain@1.5.0`, `@agent-inspect/tui@1.5.0`
**Decision:** v1.5.1 is the active corrective patch; do not start v1.6.0 until v1.5.1 is released and post-release verified.

## Executive summary

Chunks 1–3 of v1.5.1 are committed on `main`:

| Chunk | Status | Evidence |
| --- | --- | --- |
| 1 — Version and canonical metadata read path | complete | `633d89b` |
| 2 — Report safety and normalization correctness | complete | `b0ad17d` |
| 3 — Token vocabulary completion | complete | `181a619` |
| 4 — Documentation and release readiness | in progress | this docs/readiness chunk |

The package manifests remain at `1.5.0` by design. No changeset, tag, publish, or v1.6.0 implementation has been started.

## Requirement-by-requirement status

| Requirement | Status | Evidence |
| --- | --- | --- |
| Built CLI reports the public package version | complete | Chunk 1 wires the CLI version to the public root manifest and validates the built binary. |
| `list`, `stats`, and `search` use dual-format metadata reads | complete | Chunk 1 added shared v0.1/v0.2 read-path parity for metadata, stats, and search. |
| `report --redaction-profile share|strict` covers the complete report | complete | Chunk 2 builds report sections from profile-redacted structured event copies and tests Markdown/HTML output. |
| Mixed v0.1/v0.2 files preserve source order | complete | Chunk 2 normalizes valid rows inline and covers mixed ordering tests. |
| Persisted `error.name` is not mapped to v0.1 `stack` | complete | Chunk 2 preserves only real stack strings such as `attributes.errorStack`. |
| Token usage preserves `input`, `output`, `total`, and `cached` | complete | Chunk 3 adds additive vocabulary support and converter/read/summary tests. |
| No provider cost engine or token counter | complete | Chunk 3 keeps cost/pricing fields out of the canonical model and leaves arbitrary attributes opaque. |
| Stale CLI/release docs corrected | in progress | Chunk 4 updates active docs, release-train state, and readiness evidence. |
| v1.5.1 readiness evidence recorded | in progress | [V1.5.1-RELEASE-READINESS.md](../V1.5.1-RELEASE-READINESS.md). |

## Package and compatibility inventory

| Package | Version | State |
| --- | ---: | --- |
| `agent-inspect` | 1.5.0 | public baseline; no v1.5.1 version bump yet |
| `@agent-inspect/langchain` | 1.5.0 | public baseline; no v1.5.1 version bump yet |
| `@agent-inspect/tui` | 1.5.0 | public baseline; no v1.5.1 version bump yet |
| `@agent-inspect/core` | 0.1.0 | private workspace package |
| `@agent-inspect/cli` | 0.1.0 | private workspace package |

Root runtime dependencies remain the existing public set; v1.5.1 corrective work does not introduce a provider SDK, network sink, pricing dependency, or telemetry path.

## Documentation/readiness scope

Chunk 4 updates:

- public roadmap state: v1.5.0 is published, v1.5.1 is active, v1.6.0 has not started;
- CLI/API/schema/limitations wording for dual-format inspection reads and whole-report redaction;
- token vocabulary RFC semantics for `total` and `cached`;
- v1.5.0 post-publish verification record, including the known published CLI version defect;
- v1.5.1 release-readiness document and final validation matrix;
- release-train state and patch-plan execution status.

Chunk 4 must not create a changeset, mutate package versions, tag, publish, or begin v1.6.0.

## Current gate

**Gate C preparation — maintainer review pending.**

After Chunk 4 validation and commit:

1. Maintainer reviews the complete v1.5.1 patch train.
2. Maintainer decides whether to authorize Gate D release preparation.
3. Only after explicit Gate D authorization should changesets, version bumps, publish, tags, or post-publish verification occur.
