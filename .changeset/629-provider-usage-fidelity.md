---
"agent-inspect": minor
---

Provider usage fidelity: preserve cache-read (`cached`), cache-write (`cacheWrite`), and reasoning token fields across core normalization and adapters. Add isolated AI SDK 6/7 peer matrix. No pricing engine; root Node remains `>=20`.
