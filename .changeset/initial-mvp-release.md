---
"agent-inspect": patch
---

Initial MVP release of AgentInspect with local execution-tree debugging, JSONL traces, terminal output, CLI list/view, and TypeScript APIs `inspectRun`, `step`, `step.llm`, `step.tool`, and `observe`.
